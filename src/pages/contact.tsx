import Head from 'next/head';

const title = 'Contact Me';

const Page = (): JSX.Element => {
	return (<>
		<Head>
			<title>{title}</title>
		</Head>
		<header id="site_head">
		</header>
	</>);
};

export default Page;