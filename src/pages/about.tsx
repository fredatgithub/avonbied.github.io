import Head from 'next/head';

const title = 'About Me';

const Page = (): JSX.Element => {
	return (<>
		<Head>
			<title>{title}</title>
		</Head>
		<section>
			<h1></h1>
		</section>
	</>);
};

export default Page;